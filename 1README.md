#### `mkdir career`

```bash
$ git init mechanical-engineering
$ git checkout -b tech

$ pip install nxtwave-fullstack               # python, js, react, mysql
$ bench init workspace --frappe-branch main    # frappe, erpnext, vue
$ bench new-site production.local              # manufacturing ERP, 100+ users
$ git push upstream
```

#### `mkdir other`

[frappe_mail_365](https://github.com/UmakanthKaspa/frappe_mail_365) — frappe app for email via graph api.
